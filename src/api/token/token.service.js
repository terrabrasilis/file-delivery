import fs from 'fs'

const Service = {
  isValid () {
  	// based on token private key validation 
    return true
  }, 

  getProfileBasedOnToken (){
  	// validate token
  	// gettokenmetadata
  	// return public or private
  }
}

export default Service
